URL: https://github.com/edenboh/Mandatory13/blob/main/DaillyChallenge2.sql
Brief justification:
- correctness: The provided answers accurately predict the output of the SQL queries given the initial data. Q1 returns 0 because `NOT IN (SELECT ID FROM SecondTab WHERE ID IS NULL)` effectively checks `ft.id NOT IN (NULL)`, which will always evaluate to UNKNOWN, filtering out all rows. Q2 returns 2 because it checks `ft.id NOT IN (5)`, which excludes rows where `ft.id` is 5. This leaves rows where `ft.id` is 6, 7, and NULL. However, since it only counts not null id, it returns 2. Q3 returns 0 because `NOT IN (SELECT ID FROM SecondTab)` which is equivalent to `NOT IN (5, NULL)` will always evaluate to UNKNOWN if the subquery returns NULL, thus filtering out all rows. Q4 returns 2 because it checks `ft.id NOT IN (5)`, excluding the row where ft.id is 5 and counting the remaining rows that are not null ID.
- readability: N/A
- performance: N/A
- security: N/A

